[package]
name = "hexa-ir"
version = "0.1.0"
edition = "2021"
description = "HEXA-IR Compiler — n=6 native compilation stack (J₂=24 opcodes, σ=12 passes)"

[lib]
name = "hexa_ir"
path = "src/lib.rs"

[[bin]]
name = "hexa-ir"
path = "src/main.rs"

[profile.release]
opt-level = 3
