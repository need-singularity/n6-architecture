__pycache__/
*.pyc
.env
data/
# atlas canonical symlinks — tracked exception (SSOT, raw#10 proof-carrying, anima 패턴)
# 실제 symlink 는 n6shared/atlas.n6, n6shared/atlas.signals.n6 (data/ 미사용)
# → ../../data/n6/atlas{.signals,}.n6 (canonical target: ~/core/data/n6/)
!/data/n6/atlas.n6
!/data/n6/atlas.signals.n6
models/
.claude/*
!.claude/settings.json
!.claude/hooks/
.discovery-cache
# Rust 빌드 아티팩트
**/target/
.growth/

# 레거시 origins (추적 제거 완료, 재추가 방지)
bridge/origins/nexus-legacy/
bridge/origins/nexus-dashboard-legacy/

# 레거시 심링크 (TECS-L 폐기 후 잔류, 무시)
calc
.shared
model_utils.py
tecsrs

# Claude Code memory (account-shared, local only)
.claude-memory/

# macOS
.DS_Store
build/

# Lean4 build artifacts (v3 M3)
lean4-n6/.lake/
lean4-n6/lake-manifest.json

# Python virtual envs + egg-info (venv 폐기 재추적 방지, ROI#13)
venv/
*.egg-info/

# Transient hook command state (per-invocation)
.hook-commands/

# Infra state symlink → ~/core/nexus/infra_state.json (out-of-repo SSOT)
infra_state.json
