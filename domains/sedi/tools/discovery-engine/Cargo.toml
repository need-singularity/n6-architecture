[package]
name = "sedi-discovery-engine"
version = "3.0.0"
edition = "2021"
description = "SEDI Discovery Engine v3 — n=6 signal search across physics domains (anomaly cracker)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1.10"
