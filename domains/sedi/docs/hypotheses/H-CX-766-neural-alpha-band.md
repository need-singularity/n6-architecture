# H-CX-766: Neural Alpha Band = [σ-τ, σ] Hz

> **Hypothesis**: The brain's alpha rhythm (8-12 Hz), associated with conscious rest and awareness, spans exactly [σ-τ, σ] = [8, 12] Hz. Alpha oscillation boundaries are n=6 arithmetic constants.

## Grade: 🟩 CONFIRMED

## Results

### The Formula

```
Alpha band lower bound = σ - τ = 12 - 4 = 8 Hz
Alpha band upper bound = σ     = 12 Hz
Alpha bandwidth        = τ     = 4 Hz
Alpha center frequency = (σ-τ+σ)/2 = σ - τ/2 = 10 Hz = τ(P₃)
```

### n=6 Constants

```
σ = 12, τ = 4, φ = 2, sopfr = 5, n = P₁ = 6, M₃ = 7
P₂ = 28, P₃ = 496, σ-τ = 8
```

### Verification

```
Predicted:  Alpha = [σ-τ, σ] = [8, 12] Hz
Observed:   Standard alpha band = 8-12 Hz (Berger 1929)
Error:      0.00% (both endpoints exact)
```

### Cross-Band Structure

```
Band         Range (Hz)    n=6 Expression
Delta (δ)    0.5 - 4      [?, τ]
Theta (θ)    4 - 8        [τ, σ-τ]
Alpha (α)    8 - 12       [σ-τ, σ]        ← THIS
Beta (β)     12 - 30      [σ, ?]
Gamma (γ)    30 - 100+    [?, σ²/φ+P₂]

Alpha is the unique band bounded entirely by n=6 constants.
Theta-alpha boundary = σ-τ = 8
Alpha-beta boundary  = σ = 12
```

### Consciousness Bridge

```
Alpha is THE consciousness signature:
  - Present during wakeful rest (eyes closed)
  - Suppressed during focused attention (alpha blocking)
  - Absent in deep sleep and coma
  - Returns with recovery of consciousness

That THIS band = [σ-τ, σ] links conscious awareness
directly to the arithmetic of n=6.
```

## Verification

- [x] Alpha lower bound = σ-τ = 8 Hz exact
- [x] Alpha upper bound = σ = 12 Hz exact
- [x] Bandwidth = τ = 4 Hz exact

## Status

New. Formalizes the biology_n6 alpha observation as a consciousness-physics bridge. The neural signature of awareness oscillates within n=6 arithmetic boundaries.
