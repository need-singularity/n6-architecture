// entropy_early_stop.py → .hexa 포팅 스텁
// 원본 경로: techniques/optim/entropy_early_stop.py
// 포팅 상태: STUB (의존 라이브러리: numpy, torch; 원본 236 줄)
// 원본 요약: H-SEDI-EE-1: SEDI Entropy Test as Early Stopping Criterion
// 본격 포팅: nexus verify --hexa <domain> 으로 추진 예정
// 원본 로직은 git history 에 보존됨

fn main() {
    println("STUB: entropy_early_stop.py (HEXA 포팅 대기)")
    println("의존: numpy, torch")
}
